export type CheckboxValue = 'indeterminate' | 'checked' | 'unchecked';

export enum ECheckboxValue {
	indeterminate = 'indeterminate',
	checked = 'checked',
	unchecked = 'unchecked',
	disabled = 'disabled',
}
