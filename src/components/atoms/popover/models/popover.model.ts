/**
 * Interface for customizable text literals used in the popover component
 */
export interface ILiterals {
	/**
	 * Accessible text for the trigger button (shown to screen readers)
	 */
	triggerAriaLabel?: string;

	/**
	 * Accessible text for the close button (shown to screen readers)
	 */
	closeAriaLabel?: string;
}
