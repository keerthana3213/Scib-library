import { ECMVFolderNavigator } from '../component/ecmv-folder-navigator';

describe.skip('ecmv-folder-navigator', () => {
	it('builds', () => {
		expect(new ECMVFolderNavigator()).toBeTruthy();
	});
});
