import { CDKInteractionHistory } from '../component/cdk-interaction-history';

describe.skip('cdk-interaction-history', () => {
	it('builds', () => {
		expect(new CDKInteractionHistory()).toBeTruthy();
	});
});
