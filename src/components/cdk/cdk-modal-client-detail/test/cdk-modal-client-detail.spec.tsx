import { CDKModalClientDetail } from '../component/cdk-modal-client-detail';

describe.skip('cdk-modal-client-detail', () => {
	it('builds', () => {
		expect(new CDKModalClientDetail()).toBeTruthy();
	});
});
