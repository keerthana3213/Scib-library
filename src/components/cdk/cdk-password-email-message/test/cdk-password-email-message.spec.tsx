import { CDKPasswordEmailMessage } from '../component/cdk-password-email-message';

describe.skip('cdk-password-email-message', () => {
	it('builds', () => {
		expect(new CDKPasswordEmailMessage()).toBeTruthy();
	});
});
